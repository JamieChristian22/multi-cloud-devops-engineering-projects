# Cost Management

Use minimal instance sizes in dev and staging. Deploy NAT gateways and VMs only while testing. Run `terraform destroy` after demonstrations. Configure budget alerts and inspect unattached resources.
