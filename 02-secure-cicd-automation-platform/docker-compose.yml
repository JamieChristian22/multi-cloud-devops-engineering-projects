services:
 api:
  build: .
  ports: ["8080:8080"]
  environment: {ENVIRONMENT: local, APP_VERSION: compose}
  read_only: true
  tmpfs: [/tmp]
  security_opt: [no-new-privileges:true]
  cap_drop: [ALL]
