# Evidence Checklist

Capture screenshots with account IDs, credentials, tokens, and personal data hidden. Recommended evidence: successful tests and Docker build; Terraform init/validate/plan; VPC resource map; public/private subnets and routes; ECR repository and image scan; ECS cluster, task definition, running tasks, and stable service; ALB listener and healthy targets; application homepage and `/health`; CloudWatch logs and alarms; auto-scaling policies; successful GitHub Actions workflow; and successful Terraform destroy.
