# Contributing

Create a feature branch, run `make test` and `make validate`, and submit a pull request. Terraform must be formatted, validated, documented, and scoped to the project. Never commit state files, credentials, private keys, or real secret values.
