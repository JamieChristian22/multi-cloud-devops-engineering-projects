# Cost Model

Primary cost drivers are NAT gateways, the load balancer, Fargate task hours, log ingestion, WAF requests, and retained audit data. Use the included Infracost workflow with your own usage assumptions before deployment. For portfolio demonstrations, destroy resources immediately after collecting evidence.
